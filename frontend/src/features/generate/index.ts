// The three generation modes (t2v/i2v/r2v -- "Video from text" / "Provide
// first frame" / "Provide references") and the reference-asset picker will
// live here, calling the (not yet implemented) POST /api/jobs/ and
// POST /api/references/ endpoints. See ARCHITECTURE.md "deferred to next
// pass" and generation/api.py on the backend.
export {};
