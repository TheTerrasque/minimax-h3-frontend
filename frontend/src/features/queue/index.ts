// Queue/ETA display and the user's own job list/status will live here,
// calling the (not yet implemented) GET /api/queue-estimate/ and
// GET /api/jobs/ endpoints. See generation/queue.py on the backend for how
// the combined (cross-user, detail-free) ETA is computed.
export {};
