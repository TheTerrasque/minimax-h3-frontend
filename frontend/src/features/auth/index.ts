// Login/callback handling lives here: kicking off OIDC login (redirect to
// the Django-served /accounts/... URLs -- session cookie auth, see
// src/api/client.ts) and reading the current user from a future
// GET /api/me/ endpoint. Not implemented yet -- see ARCHITECTURE.md
// "deferred to next pass".
export {};
