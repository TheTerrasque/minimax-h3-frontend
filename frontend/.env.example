# Base path the SPA calls for API requests. Under the Docker Compose stack,
# nginx proxies /api/ to the backend on the same origin, so the default
# (relative "/api") is normally correct as-is.
VITE_API_BASE_URL=/api
